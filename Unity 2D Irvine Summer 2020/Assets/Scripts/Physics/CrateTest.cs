using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrateTest : PhysicsObject
{
    // Start is called before the first frame update
    void Start()
    {
        targetVelocity = Vector2.left;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
